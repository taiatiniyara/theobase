# Contributing

## Development

```bash
npm install
npm run dev
```

## Quality Gates

Before pushing, ensure:

```bash
npm run typecheck   # Must exit 0
npm run lint        # Must exit 0
npm run format      # Must exit 0
```

## Commit Style

Follow the repository conventions. Write meaningful commit messages.
