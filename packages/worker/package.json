{
  "name": "@theobase/worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "test": "vitest run",
    "db:generate": "drizzle-kit generate",
    "seed:demo": "npx wrangler --env prod secret:seed || (echo 'Ensure wrangler is configured and the worker is deployed, then run: curl -X POST https://theobase-worker.theobase.workers.dev/church/seed-demo')"
  },
  "dependencies": {
    "@theobase/shared": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "jose": "^6.2.8"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.20.3",
    "@cloudflare/workers-types": "^4.20250808.0",
    "drizzle-kit": "^0.31.10",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "^4.52.1"
  }
}
