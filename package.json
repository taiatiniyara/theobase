{
  "name": "theobase",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run",
    "test:do": "vitest run --config vitest.do.config.ts",
    "test:relay": "vitest run --config vitest.relay.config.ts",
    "test:all": "vitest run && vitest run --config vitest.do.config.ts && vitest run --config vitest.relay.config.ts",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8",
    "@cloudflare/workers-types": "^4.20250600.0",
    "@eslint/js": "^10.0.1",
    "@types/better-sqlite3": "^7.6.13",
    "eslint": "^10.5.0",
    "miniflare": "^4.20260617.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.61.1",
    "vitest": "^3.0"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "drizzle-kit": "^0.30.0",
    "drizzle-orm": "^0.38.0"
  }
}
