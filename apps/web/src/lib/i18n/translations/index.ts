import en from "./en";
import fr from "./fr";
import es from "./es";
import pt from "./pt";
import sw from "./sw";
import type { TranslationDict } from "../types";

export const translations: Record<string, TranslationDict> = {
  en,
  fr,
  es,
  pt,
  sw,
};
