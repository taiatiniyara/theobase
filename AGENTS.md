## Agent skills

### Issue tracker

Issues live as GitHub issues in `taiatiniyara/theobase`; skills use the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout: `CONTEXT.md` at the repo root, `docs/adr/` for decisions. See `docs/agents/domain.md`.
